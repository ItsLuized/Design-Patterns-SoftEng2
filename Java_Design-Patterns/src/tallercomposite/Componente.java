package tallercomposite;

public interface Componente {

    /**
     * PROBLEMA: Tienes diferentes usuarios con estas variables (nombre, direccion, clave, correo y teléfono),
     * y quieres saber por ejemplo si van a la universidad ,
     * de donde vienen, es decir, si desde Chía o Bogotá
     */

    void lugar(String ciudad);
}
